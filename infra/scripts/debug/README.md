# Debug / experimental scripts

HEASARC TAP debug and experimental fetchers (moved from services/sky/pipelines/misc/).

Run from repo root with `PYTHONPATH=services/sky`:

```bash
PYTHONPATH=services/sky python infra/scripts/debug/debug_grb_heasarc.py
```
